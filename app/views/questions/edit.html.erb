<h1>Edit Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for questions</p>
    <ul>
      <li>Make sure it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |p| %>
    <div class="form-group">
      <%= p.label :title %>
        <%= p.text_field :title, class: 'form-control', placeholder: "Enter question title" %>
      </div>
      <div class="form-group">
        <%= p.label :body %>
        <%= p.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body" %>
      </div>
      <div class="form-group">
        Mark as Resolved? <%= p.check_box :resolved %>
      </div>
      <div class="form-group">
        <%= p.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
